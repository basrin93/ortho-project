# Руководство по миграциям Prisma для Timeweb

## 1. Настройка DATABASE_URL

Создайте файл `.env` в папке `backend/` (если его еще нет) и добавьте строку подключения к базе данных Timeweb:

```env
DATABASE_URL="postgresql://username:password@host:port/database?sslmode=require"
```

### Как получить данные для подключения в Timeweb:

1. Войдите в панель управления Timeweb
2. Перейдите в раздел "Базы данных" → "PostgreSQL"
3. Найдите вашу базу данных
4. Скопируйте данные подключения:
   - **Хост** (host) - обычно выглядит как `pXXXXXX.timeweb.ru` или IP-адрес
   - **Порт** - обычно `5432` для PostgreSQL
   - **Имя базы данных** (database)
   - **Пользователь** (username)
   - **Пароль** (password)

### Пример DATABASE_URL:

```env
DATABASE_URL="postgresql://user123:mypassword@p123456.timeweb.ru:5432/ortho_db?sslmode=require"
```

**Важно:** 
- Параметр `?sslmode=require` обязателен для безопасного подключения
- Не коммитьте файл `.env` в Git! Добавьте его в `.gitignore`

## 2. Создание миграции

После изменения схемы Prisma (`schema.prisma`) выполните:

```bash
cd backend
npm run prisma:migrate
```

Или напрямую:

```bash
npx prisma migrate dev --name add_templates_and_visit_photos
```

Эта команда:
- Создаст новую миграцию в папке `prisma/migrations/`
- Применит миграцию к базе данных
- Сгенерирует Prisma Client

## 3. Применение миграции на продакшене

Если вы уже создали миграцию локально и хотите применить её к базе данных Timeweb:

```bash
npm run prisma:migrate:deploy
```

Или:

```bash
npx prisma migrate deploy
```

Эта команда применяет все неприменённые миграции без создания новых.

## 4. Генерация Prisma Client

После применения миграции всегда нужно сгенерировать клиент:

```bash
npm run prisma:generate
```

Или:

```bash
npx prisma generate
```

## 5. Просмотр базы данных

Для визуального просмотра данных в базе:

```bash
npm run prisma:studio
```

Или:

```bash
npx prisma studio
```

Откроется веб-интерфейс на `http://localhost:5555`

## 6. Полный процесс для новой миграции

1. Измените `prisma/schema.prisma`
2. Создайте и примените миграцию:
   ```bash
   npm run prisma:migrate
   ```
3. Prisma Client автоматически сгенерируется
4. Перезапустите приложение

## 7. Проверка статуса миграций

Чтобы проверить, какие миграции применены:

```bash
npx prisma migrate status
```

## Важные замечания

⚠️ **Перед применением миграции на продакшене:**
- Сделайте резервную копию базы данных
- Проверьте миграцию на тестовой базе
- Убедитесь, что `DATABASE_URL` в `.env` указывает на правильную базу

🔒 **Безопасность:**
- Никогда не храните `.env` файл в репозитории
- Используйте переменные окружения на сервере
- Используйте SSL подключение (`sslmode=require`)

